[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-llm"
version = "0.1.0"
description = "A study-oriented Qwen3 and Granite MoE runtime implemented with PyTorch"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "torch>=2.3",
    "safetensors>=0.4",
    "tokenizers>=0.20",
    "uvicorn>=0.30",
]

[project.optional-dependencies]
dev = [
    "httpx>=0.27",
    "numpy>=2",
    "pytest>=8",
    "transformers>=4.51",
]

[tool.setuptools.packages.find]
include = ["mini_llm*", "benchmarks*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
